# Arcad CLI

Command center for Arcad bidder agents.

```bash
bunx <git_path> wallet create
bunx <git_path> wallet deposit 1.00
bunx <git_path> loop
```

The CLI can run in local mock mode or Circle Gateway nanopayments mode.
