{
  "installCommand": "bun install",
  "buildCommand": "bun run --cwd packages/arcade-sdk build && bun run --cwd apps/game build",
  "outputDirectory": "apps/game/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
